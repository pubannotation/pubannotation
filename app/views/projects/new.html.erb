<% content_for :path do %>
	> <%= link_to "home", home_path %>
	<% if @sourcedb == 'PubMed' %>
		> <%= link_to "pmdocs", pmdocs_path %>
		> <%= link_to @sourceid, pmdoc_path(@sourceid) %>
	 	> <%= link_to "projects", pmdoc_projects_path(@sourceid) %>
	<% elsif @sourcedb == 'PMC' %>
		> <%= link_to "pmcdocs", pmdocs_path %>
		> <%= link_to @sourceid, pmdoc_path(@sourceid) %>
	 	> <%= link_to "projects", pmdoc_div_projects_path(@sourceid, @serial) %>
	<% end %>
 	> <%= link_to "projects", projects_path %>
 	> new
<% end %>

<h1>New annotation set/project</h1>

<%= render 'form' %>
