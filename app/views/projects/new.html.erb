<% content_for :path do %>
	> <%= link_to t('views.paths.home'), home_path %>
 	> <%= link_to "projects", projects_path %>
 	> <%= t('views.paths.new') %>
<% end %>

<h1>
	<%= t('views.shared.new_model', :model => t('activerecord.models.project')) %>
	<span class="button"><%= link_to t('views.shared.create_model', :model => t("activerecord.models.sproject")), new_sproject_path -%></span>
</h1>

<%= render :partial => 'form', :locals => {:model => @project}  %>
