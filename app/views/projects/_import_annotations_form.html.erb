<%= form_tag(import_project_annotations_path(@project.name), :method => "post") do %>
	<%= render partial: "select_project", locals: {placeholder: "project to import anns from"} %>
	<button type="submit" class="short-button" title="Import"><i class="fa fa-share-square-o" aria-hidden="true"></i></button>
<% end %>
