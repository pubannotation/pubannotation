<%= form_tag(compare_project_path(@project.name), :method => "post") do %>
	<%= render partial: "select_project", locals: {placeholder: "project to compare to"} %>
	<button type="submit" class="short_button" title="Compare"><i class="fa fa-balance-scale" aria-hidden="true"></i></button>
<% end %>
