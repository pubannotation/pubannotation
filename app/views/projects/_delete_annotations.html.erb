<h3>Batch delete</h3>

<%= link_to 'Delete all annotations', project_destroy_annotations_path(@project.name), method: :delete, class: 'button', confirm: "Are you sure? All the annotations in the project will be deleted." %>
