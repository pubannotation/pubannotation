<p><%= t('devise.mailer.reset_password_instructions.hello', :email => @resource.email) %></p>

<p>t('devise.mailer.reset_password_instructions.requested')</p>

<p><%= link_to t('devise.mailer.reset_password_instructions.change_my_password'), edit_password_url(@resource, :reset_password_token => @resource.reset_password_token) %></p>

<p><%= t('devise.mailer.reset_password_instructions.description').html_safe %></p>
