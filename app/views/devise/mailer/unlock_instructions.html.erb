<p><%= t('devise.mailer.unlock_instructions.hello', :email => @resource.email) %></p>

<p><%= t('devise.mailer.unlock_instructions.locked') %></p>

<p><%= t('devise.mailer.unlock_instructions.unlock_link') %></p>

<p><%= link_to t('devise.mailer.unlock_instructions.unlock_my_account'), unlock_url(@resource, :unlock_token => @resource.unlock_token) %></p>
