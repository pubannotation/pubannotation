<% content_for :path do %>
  > <%= link_to t('views.paths.home'), home_path %>
  <% if @project %>
    > <%= link_to "projects", projects_path %>
    > <%= link_to @project.name, project_path(@project.name) %>
  <% end %>
  > pmdocs
<% end %>

<%= render :partial => "list", :locals => {:docs => @docs, :project => @project, :denotations_count_order => 'denotations_count'} %>
