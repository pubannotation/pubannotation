<h2>See the annotations in</h2>
<table style="border-color:#FFFFFF; margin-left:20px">
<tr>
	<th><%= t('views.shared.see') %></th>
	<td>in</td>
  <td>[<%= link_to "Table", annotations_url -%>]</td>
	<td>[<%= link_to "JSON",  annotations_url + '.json' %>]</td>
  <td>[<%= visualization_link(@span) -%>]</td>
</tr>

<% unless @project.editor.blank? %>
	<tr>
		<th><%= t('views.shared.open') %></th>
		<td>in</td>
		<% if @project.editor.include?('?') %>
			<td>[<a href="<%= @project.editor %>&target=<%= annotations_url + '.json' %>">Editor</a>]</td>
		<% else %>
			<td>[<a href="<%= @project.editor %>?target=<%= annotations_url + '.json' %>">Editor</a>]</td>
		<% end %>
	</tr>
<% end %>

<% unless @project.rdfwriter.blank? and @project.xmlwriter.blank? and @project.bionlpwriter.blank? %>
	<tr>
		<th><%= t('views.shared.get') %></th>
		<td>in</td>
		<% unless @project.rdfwriter.blank? %>
			<td>[<%= link_to "RDF",  annotations_url + '.ttl' %>]</td>
		<% end %>

		<% unless @project.xmlwriter.blank? %>
			<td>[<%= link_to "XML",  annotations_url + '.xml' %>]</td>
		<% end %>

		<% unless @project.bionlpwriter.blank? %>

			<!--script type="text/javascript">
				// function getconversion(name) {
				// 	xmlhttp = new XMLHttpRequest();
				// 	xmlhttp.open('GET', '<%= annotations_url %>' + '.json', false);
				// 	xmlhttp.send();
				// 	var json = xmlhttp.responseText;
				// 	document.getElementById(name).children[2].setAttribute("value", json);
				// 	document.forms[name].submit();
				// }
			</script>

	 		<td>
				<%= form_tag(project.bionlpwriter, :method => "post", :id => "getbionlp") do %>
					<%= hidden_field_tag(:identifier, identifier) %>
					<%= hidden_field_tag(:data, "") %>
					[<a onclick="getconversion('getbionlp')" href="#">BioNLP-ST</a>]
				<% end %>
			</td>
	 -->
	 	<% end %>

	</tr>
<% end %>

</table>
